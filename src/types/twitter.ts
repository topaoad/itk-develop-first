export type twitterUser = {
  profile_image_url: string;
  name: string;
  username: string;
};

export type twitterTweets = {
  created_at: string;
  author_id: string;
  text: string;
  id: string;
};
