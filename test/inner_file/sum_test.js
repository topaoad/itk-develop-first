// import sum from "./sum";
// common js 形式
// グローバル化しているので、require不要
// const { expected, test } = require("../setup");

test("adds 1 + 2 to equal 3", () => {
  expected(1, 2, 3);
});
test("adds 1 + 2 to equal 3", () => {
  expected(1, 2, 8);
});
